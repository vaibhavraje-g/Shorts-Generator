const chatbotTemplate = {
  name: 'Chatbot',
  prompt: `You are a helpful AI assistant. Respond to the following message in a concise and friendly manner: {userMessage}`,
};

module.exports = chatbotTemplate;